- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.zshrc:
    ~/.aliases:
    ~/.gitconfig:
    ~/.gitignore:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
