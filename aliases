# abbr for better listing
alias ll="ls -al"

# -v is the verbose option
alias ln="ln -v"

# check with man mkdir please
alias mkdir="mkdir -p"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# Include custom aliases
if [[ -f ~/.aliases.local ]]; then
  source ~/.aliases.local
fi